pub fn solve(data: &str) -> Result<u32, &'static str> {
   todo!() 
}

#[cfg(test)]
mod tests {
    use crate::part2::solve;

    #[test]
    fn test_example() {
        let input = "";

        let res = solve(input).unwrap();

        assert_eq!(res, TODO)
    }
}
